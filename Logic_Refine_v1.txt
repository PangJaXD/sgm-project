logic refine and set up for the first version of application
this is the logic that should be implement in this system

Login or Creating user requirement
ชื่อผู้ใช้ (Username)
1. ต้องเป็นภาษาอังกฤษหรือตัวเลข รวมอักขระพิเศษ [ !#_.- ]
2. มีความยาวตั้งแต่ 4 ตัวอักษร และไม่เกิน 30 ตัวอักษร
3. ต้องไม่มีเว้นวรรค หรือช่องว่าง
4. ต้องไม่เป็นค่าว่าง

รหัสผ่าน (password)
1. ต้องเป็นตัวอักษรภาษาอังกฤษหรือตัวเลข รวมอักขระพิเศษ[ !#_. ]
2. มีความยาวตั้งแต่ 8 ตัวอักษร และไม่เกิน 16 ตัวอักษร
3. ต้องไม่มีเว้นวรรค หรือช่องว่าง
4. ต้องไม่เป็นค่าว่าง

this implement in every field that related


WEBSITE
-headguard and guard
get new status call suspended and fired
suspended can get back to work
and fired cannot be query in the homepage
-Admin
making a admin dashboard
base on company and headguard dashboard

-event
must sort with the ongoing event first follow by pending and the others
if event ended the status should be เสร็จสิ้น or something close to the meaning
start date default value should be the date right now (start at 0:00 am) and cannot be the day before
if there is a upcoming event the notification must be send to all the guard that thier headguard got apply for the event
shift date must be in the event time the time cannot overlap
-guard request
the shift should be fully max and have reserve guard half of the shift
if someone withdraw the first reserved guard automatically become applied
if the shift is not fully max and the reserved guard is empty the notification must be send to all guard
that headguad in this shift responsible for
please format the time 0.00-24.00 i saw format problem on website

Mobile
-guard
guard cannot do anything on the event if they did not get approve yet
they can report or do any action in the event if they are on the shift
SOS report should be show in any state of the phone(High Priority) and request access in the manifest
the score should be calculate with work history, are they coming on time and sign out at the end of the shift

Set up
-making the data of admin company headguard guard about 20 each
-each company get 10 event(the data will have all kind of status and the start date start with today)
-guard will have the finish event data and thier report to the head


When completing a task, strictly follow this sequence:
1. Run local tests and security checks (e.g., test suites, linter). Ensure no compile/runtime errors.
2. Check `git status` and `git diff` to verify only intended files are changed.
3. Ensure no API keys, tokens, or `.env` files are tracked by Git.
4. Do NOT commit directly to `main` or `master`. Checkout a new task branch: `git checkout -b feature/<task-name>`.
5. Stage files: `git add <changed-files>`
6. Commit with a clear Conventional Commit message: `git commit -m "..."`
7. Push to the remote branch: `git push -u origin <branch-name>`